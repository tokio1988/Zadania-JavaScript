document.addEventListener("DOMContentLoaded",function(){

    var option = document.querySelector(".form-control")

    var optionChildren = option.children

    function changeImage()
    {
        if (optionChildren[0].selected == true)
        {
            
        }
    }

})